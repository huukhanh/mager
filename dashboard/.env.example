# Dev: leave unset to use Vite proxy → http://127.0.0.1:8787 (wrangler dev).
# Production build (Pages): set to your Worker URL, e.g. https://<name>-mager.<subdomain>.workers.dev
# VITE_API_BASE_URL=
